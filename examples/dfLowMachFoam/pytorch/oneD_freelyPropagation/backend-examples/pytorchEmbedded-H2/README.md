# pytorchEmbedded-H2

Self-contained runnable H2 case using the embedded-Python backend.

This case keeps the case-local `inference.py` module and is configured to run to
`endTime = 0.01`.

Run with:

```bash
./Allrun
```
